# Outputs Directory

This directory contains temporary runtime-generated visual outputs and heatmaps (such as `heatmap_latest.html`) created by the SurakshaAI Safety Dashboard.

All visual HTML/rendering outputs inside this directory are transient and are ignored by Git.
